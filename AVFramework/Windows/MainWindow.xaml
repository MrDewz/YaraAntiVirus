<Window x:Class="AVFramework.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AVFramework.Windows"
        xmlns:classes="clr-namespace:AVFramework.Classes"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="YaraScanner" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF1E1E1E" StateChanged="Window_StateChanged">

    <Window.Resources>
        <!-- Стиль для кнопок -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Gray"/>
            <Setter Property="Foreground" Value="white"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="5">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="5" 
                                                ShadowDepth="1" 
                                                Opacity="0.2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFE0E0E0" Offset="0"/>
                                <GradientStop Color="#FFD3D3D3" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="8" 
                                            ShadowDepth="2" 
                                            Opacity="0.3"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для текстовых блоков -->
        <Style x:Key="ModernTextBlock" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="2" 
                                    ShadowDepth="1" 
                                    Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для RichTextBox -->
        <Style x:Key="ModernRichTextBox" TargetType="RichTextBox">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RichTextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="5">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" 
                                                ShadowDepth="2" 
                                                Opacity="0.2"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ProgressBar -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="#FFE0E0E0" 
                                BorderThickness="0" 
                                CornerRadius="3">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="5" 
                                                ShadowDepth="1" 
                                                Opacity="0.2"/>
                            </Border.Effect>
                            <Grid>
                                <Border x:Name="PART_Track" 
                                        Background="#FFE0E0E0"/>
                                <Border x:Name="PART_Indicator" 
                                        Background="#FFD3D3D3" 
                                        HorizontalAlignment="Left">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="3" 
                                                        ShadowDepth="1" 
                                                        Opacity="0.3"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="#FFD3D3D3"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="10">
                            <Grid>
                                <Border x:Name="PART_Track" 
                                        Background="{TemplateBinding Background}"
                                        BorderThickness="0"/>
                                <Border x:Name="PART_Indicator"
                                        Background="{TemplateBinding Foreground}"
                                        BorderThickness="0"
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <StackPanel Grid.Row="0" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,10">
            <Border Background="#FFD3D3D3" 
                    CornerRadius="5"
                    Margin="10,10,10,10" Width="720">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" 
                                    ShadowDepth="2" 
                                    Opacity="0.2"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" 
                          Margin="10">
                    <Button x:Name="ChooseFileBtn" 
                            Content="{StaticResource ChooseFile}" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="ChooseFileBtn_Click" Height="24" Width="105"/>
                    <Button x:Name="ScanFolderBtn" 
                            Content="Выбрать папку" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="Button_Click" Height="24" Width="100"/>
                    <Button x:Name="ScanBtn" 
                            Content="{StaticResource Scan}" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="ScanBtn_Click" Height="24" Width="90"/>
                    <Button x:Name="SettingsBtn" 
                            Content="{StaticResource Settings}" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="SettingsBtn_Click" Height="24" Width="80"/>
                    <Button x:Name="QuarantineBtn" 
                            Content="{StaticResource Quarantine}" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="QuarantineBtn_Click" Height="24" Width="70"/>
                    <Button x:Name="ReportsBtn" 
                            Content="{StaticResource Reports}" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="ReportsBtn_Click" Height="24" Width="60"/>
                    <Button x:Name="VirusReportBtn" 
                            Content="Отправить отчет" 
                            Style="{StaticResource ModernButton}"
                            Foreground="Black"
                            Click="VirusReport_Click" Height="24" Width="120"/>
                    <Separator Width="20" Visibility="Hidden"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Основная область -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Текущая задача -->
            <TextBlock x:Name="CurrentTask" 
                       Text="Готов к сканированию" 
                       Style="{StaticResource ModernTextBlock}"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="White"/>

            <!-- Прогресс сканирования -->
            <Grid Grid.Row="1" Margin="0,10,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Общий прогресс -->
                <ProgressBar x:Name="OverallProgressBar"
                            Style="{StaticResource ModernProgressBar}"
                            Height="NaN"
                            Margin="0,-11,0,5"/>

                <!-- Детальный прогресс -->
                <ProgressBar x:Name="DetailedProgressBar"
                            Style="{StaticResource ModernProgressBar}"
                            Height="15"
                            Grid.Row="1"/>

                <!-- Текстовые описания прогресса -->
                <TextBlock x:Name="OverallProgressText"
                          Text="Общий прогресс: 0%"
                          Foreground="Black"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Margin="0,0,0,5"/>

                <TextBlock x:Name="DetailedProgressText"
                          Text="Текущий файл: 0%"
                          Foreground="Black"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Grid.Row="1"/>
            </Grid>

            <!-- Лог сканирования -->
            <Border Grid.Row="2" 
                    Background="#FF2D2D2D" 
                    CornerRadius="4">
                <RichTextBox x:Name="LogBox" 
                            IsReadOnly="True"
                            VerticalScrollBarVisibility="Auto"
                            Style="{StaticResource ModernRichTextBox}"/>
            </Border>
        </Grid>

        <!-- Нижняя панель -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Настройки -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        Margin="0,10,0,0"
                        VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
